# Reclaim AI React (Next.js) frontend
# Copy to `.env.local` before `npm run dev`.
# Never commit `.env.local`.

# Express AI backend base URL (no trailing slash)
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000
